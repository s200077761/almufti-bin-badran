# المساهمة في المفتي بن بدران
# Contributing to Almufti Bin Badran

شكراً لاهتمامك بالمساهمة في مشروع المفتي بن بدران! نرحب بجميع المساهمات سواء كانت:

- إصلاح الأخطاء (Bug Fixes)
- إضافة ميزات جديدة (New Features)
- تحسين الوثائق (Documentation)
- تحسين الأداء (Performance Improvements)

## خطوات المساهمة

### 1. Fork المستودع
```bash
git clone https://github.com/yourusername/almufti-bin-badran.git
cd almufti-bin-badran
```

### 2. إنشاء فرع جديد
```bash
git checkout -b feature/your-feature-name
# أو للإصلاحات
git checkout -b fix/your-bug-fix
```

### 3. إجراء التغييرات
- اتبع معايير الكود الموضحة أدناه
- أضف اختبارات للميزات الجديدة
- حدّث الوثائق إذا لزم الأمر

### 4. الاختبار
```bash
# تشغيل جميع الاختبارات
pytest tests/

# مع تقرير التغطية
pytest tests/ --cov=almufti
```

### 5. Commit التغييرات
```bash
git add .
git commit -m "Add: description of your changes"
```

### 6. Push والفتح Pull Request
```bash
git push origin feature/your-feature-name
```

## معايير الكود

### أسلوب الكود
- استخدم **Black** لتنسيق الكود
- استخدم **flake8** للتحقق من الأخطاء
- اتبع **PEP 8** في الأسلوب

```bash
# تنسيق الكود
black almufti/

# التحقق من الأخطاء
flake8 almufti/
```

### التعليقات والوثائق
- أضف تعليقات واضحة للكود المعقد
- استخدم docstrings للدوال والفئات
- اكتب بالعربية والإنجليزية عند الحاجة

```python
def example_function(param1: str, param2: int) -> str:
    """
    وصف الدالة بالعربية
    Description of the function in English
    
    Args:
        param1: وصف المعامل الأول
        param2: وصف المعامل الثاني
        
    Returns:
        وصف القيمة المرجعة
    """
    pass
```

### الاختبارات
- اكتب اختبارات لكل ميزة جديدة
- تأكد من أن جميع الاختبارات تمر
- استهدف نسبة تغطية 80% على الأقل

```python
def test_new_feature():
    """اختبار الميزة الجديدة"""
    result = new_feature()
    assert result is not None
```

## أنواع الـ Commits

استخدم البادئات التالية:

- **feat**: ميزة جديدة
- **fix**: إصلاح خطأ
- **docs**: تحديث الوثائق
- **style**: تنسيق الكود (بدون تغيير الوظيفة)
- **refactor**: إعادة هيكلة الكود
- **perf**: تحسين الأداء
- **test**: إضافة أو تحديث الاختبارات
- **chore**: تحديثات البناء والتبعيات

أمثلة:
```bash
git commit -m "feat: add Arabic language support to chat engine"
git commit -m "fix: resolve memory leak in database manager"
git commit -m "docs: update installation instructions"
```

## سياسة الفروع

- `master`: الإصدار المستقر
- `develop`: فرع التطوير الرئيسي
- `feature/*`: فروع الميزات الجديدة
- `fix/*`: فروع الإصلاحات
- `docs/*`: فروع تحديثات الوثائق

## عملية المراجعة

1. سيتم مراجعة PR الخاص بك من قبل المشرفين
2. قد نطلب تغييرات إضافية
3. عند الموافقة، سيتم دمج PR الخاص بك
4. شكراً على مساهمتك!

## الإبلاغ عن الأخطاء

إذا وجدت خطأ:

1. تحقق من أنه لم يتم الإبلاغ عنه بالفعل
2. أنشئ issue جديد مع:
   - وصف واضح للخطأ
   - خطوات إعادة الإنتاج
   - السلوك المتوقع والفعلي
   - معلومات النظام (OS, Python version, إلخ)

## طلبات الميزات

لاقتراح ميزة جديدة:

1. تحقق من أنها لم تُقترح بالفعل
2. أنشئ issue مع:
   - وصف الميزة المقترحة
   - الفائدة المتوقعة
   - أمثلة الاستخدام

## قواعس السلوك

- كن محترماً وودياً
- تقبل النقد البناء
- ركز على الكود وليس الشخص
- ساعد الآخرين على التعلم

## الأسئلة؟

- 📧 البريد الإلكتروني: dev@almufti.ai
- 💬 المنتدى: [Community Forum]
- 🐛 الإبلاغ عن الأخطاء: [GitHub Issues]

---

**شكراً لمساهمتك! 🙏**
